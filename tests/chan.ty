import os (..)

let chan = Channel()

Thread(isolated=true, fn () {
    sleep(0.05)
    chan.send('PASS')
})

if let Some(x) = chan.recv(0) {
    print('FAIL')
}

if let Some(x) = chan.recv() {
    print(x)
}
