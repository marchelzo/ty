import os (..)

ns test

pub fn chan() {
    let ch = Channel()
    Thread(isolated=true, fn () {
        sleep(0.05)
        ch.send('hello')
    })
    assert(ch.recv(0) == None)
    assert(ch.recv() == Some('hello'))
}
