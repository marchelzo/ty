import ty

ns test

pub fn constraint() {
    fn f(x: Int, s: String) -> Float { return 0.4 }
    f(10, 'foo')
    try {
        __eval__('f(10, 20)')
        assert(ty.q)
    } catch ex :: 'invalid argument type' in ex.what {
        ;
    }
}
