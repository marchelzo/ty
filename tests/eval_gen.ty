import support.process (FIXTURES)

ns test

class Foo {
    foo*(x) {
        yield __eval__(x)
    }
}

pub fn evgen() {
    for f in Foo().foo((FIXTURES / 'eval-upper.ty').read-text()) {
        assert(f('pass') == 'PASS')
    }
}
