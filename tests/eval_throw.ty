import ty

ns test

pub fn evthrow() {
    let Ok(mod) = ty.parse('throw 0') or assert(false)
    try {
        ty.eval(ty.Multi(mod.ast))
        assert(false)
    } catch ex {
        assert(ex == 0)
    }
}
