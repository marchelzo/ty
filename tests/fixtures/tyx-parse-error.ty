import ink (..)
import term
class Repro < Component {
    render() -> Node { <Box><Spinner label="x" /></Box> }
    on-event(ev) {
        use term.event
        match ev { Text('q') => return Exit, _ => () }
    }
}
