let x = nil


match [4, 5, 6] {
    [4, >x, -1] => {
    },

    [>x] => {
    },

    [_, _, >x, *] => {
    }
}

if x != 6 { print('FAIL') }

if not let Some([_, >x]) = Some([0, 1]) {
    print('FAIL')
}

if x != 1 { print('FAIL') }

let [_, _, >x] = [*'based']

print(match x {
    's' => 'PASS',
    _   => 'FAIL'
})
