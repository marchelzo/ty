import sh (sh)

if not let $out = sh('echo -n based 1>&2', combineOutput: true) or out != 'based' {
    print('FAIL')
}

if not let $out = sh('echo -n based') {
	print('FAIL')
} else {
	print(out == 'based' ? 'PASS' : 'FAIL')
}

