ns test

pub fn sugar() {
    let f = \match _ {
        [::_, _, y] => 0,
        _           => 1
    }
    assert(f([5, 9999, 5], 5) == 0)
    assert(f([5, 5, 5], 4) == 1)
    assert((..100).map\(_ + 1).fold\(_ + _) == 5050)
}
