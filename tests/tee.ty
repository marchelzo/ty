ns test

pub fn tee() {
    let rng = generator {
        while true {
            yield rand()
        }
    }
    let (r1, r2, r3) = Iter.tee(rng.take(50), n=3)
    assert([*r1].len() == 50)
    assert([*r2] == [*r3])
}
