import tp (..)

let pool = ThreadPool(8, function (s) {
  for _ in ..2500 { 
    s.chars().shuffle().sum()
  }

  return s
})

let futures = []

for _ in ..250 {
  futures.push(pool.submit('PASS'))
}

print(futures[-1].wait())
