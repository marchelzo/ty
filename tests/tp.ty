import os
import tp (..)

let pool = ThreadPool(os.ncpu(), function (s) {
  for i in ..500 {
    s.chars().shuffle().sum()
  }

  return s
})

let futures = []

for _ in ..64 {
  futures.push(pool.submit('PASS'))
}

print(futures[-1].wait())
