import os
import tp (..)

let pool = ThreadPool(os.ncpu(), fn (s) {
    for ..300 {
        s.chars().shuffle().sum()
    }
    s
})

let futures = []

for _ in ..64 {
  futures.push(pool.submit('PASS'))
}

print(futures[-1].wait())
