ns test

use match Twice {
    (k: Int) and (k % 2 == 0) => k / 2
}

pub fn unapply() {
    let result = match 60 {
        Twice(Twice(15)) => true,
        _                => false
    }
    assert(result)
}
