ns test

pub fn yield-from-c() {
    let g = nil
    let s = ''
    fn add(c) { s += (yield c)(c) }
    g = generator {
        'pass:fail'.chars().map(add)
    }
    fn f() {
        g(&upper)
    }
    [*f.take(5)]
    assert(s == 'PASS')
}
