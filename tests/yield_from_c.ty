ns test

fn gen*(fun) {
    let cs = 'pass:fail'.chars()
    cs.map(fun)
}

pub fn yield-from-c() {
    let s = ''

    fn add(c) { s += (yield c)(c) }

    let g = gen(add)

    fn f() {
        g(&upper)
    }

    [*f.take(5)]

    assert(s == 'PASS')
}
